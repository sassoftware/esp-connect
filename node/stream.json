[
    {
        "info": {
            "defaults": {
                "format":"ubjson",
                "interval":"0",
                "maxevents":"0",
                "schema":"yes"
            },

            "usage":{
                "usage":"-window [-format] [-interval] [-maxevents] [-blocksize] [-schema]",
                "options":{
                    "window":"ESP window in the form of project/contquery/window",
                    "format":"xml | json | ubjson (defaults to ubjson)",
                    "interval":"the interval, in milliseconds, at which to deliver events (defaults to 0 which delivers events as they occur)",
                    "maxevents":"the maximum number of events to deliver at any one time (delivers to 0 which means no maximum)",
                    "schema":"true | false (return schema on start, defaults to true)"
                }
            }
        }
    },

    {
        "event-stream": {
            "id":"#guid#",
            "window":"@window@",
            "format":"@format@",
            "interval":"@interval@",
            "maxevents":"@maxevents@",
            "schema":"@schema@"
        }
    }
]
